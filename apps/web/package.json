{
  "dependencies": {
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:"
  },
  "name": "web",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "vite",
    "fetch:articles": "node ./scripts/fetch-substack-articles.mjs",
    "fetch:projects": "node ./scripts/fetch-github-projects.mjs",
    "prebuild": "pnpm run fetch:projects && pnpm run fetch:articles",
    "predev": "pnpm run fetch:projects && pnpm run fetch:articles",
    "preview": "vite preview"
  },
  "type": "module",
  "version": "1.0.0"
}
