import type { EmploymentRole } from "@/types/portfolio.types";

/**
 * Props for a single experience timeline entry.
 */
export interface ExperienceItemProps {
  /** Employment role metadata. */
  role: EmploymentRole;
}
